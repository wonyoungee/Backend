모델(순수한 c#코드로 만듦) 단 안에 만드는 것
1. service
2.dao
3.dto

dataset => 소형의 DB

write.aspx -> List.aspx 	    -> 	        	Detail.aspx 	      -> 	      Edit.aspx      /     Delete.aspx
(insert)	      (select)    num(중간매개체)       (select where num)	    num	   (select, update)	     (delete)

------------------------------------------------------------------------------------

<계층형게시판(답글)>
ref - 원본글
depth - 들여쓰기
step - 순서


---------------------------------------------------------------------------------------

* 인증 : 회원이냐 아니냐 - 회원가입 되어있으면 인증

* 권한 : 특정 페이지에 접근

* 로그인 : id/pwd 인증 - session["userid"] = "kglim"

--------------------------------------------------------------
답글일경우
update note set step = step + 1
where ref = (현재 읽은 ref) and step > (현재 읽은 step)

insert into note(ref, depth, step)
values(10, 현재읽은원본글depth+1, 현재읽은원본글step+1)

---------------------------------------------------------------

<계층형게시판>
1. 원본글 (답글 있는 경우 / 없는 경우)
1.1 답글 없는 경우 : 삭제
1.2 답글 있는 경우 (삭제, Flag 삭제 여부) 모든 글 Y, 삭제되면 N

(지워진글)
->답글

1.3 답글이 있으면 삭제 금지인 경우

1.4 답글까지 모두 삭제

--------------------------------------------------------------------
